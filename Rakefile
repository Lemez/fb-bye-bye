require 'rake'
require 'bundler'

SCRIPT_ROOT = "/Users/JW/Websites/facebook-replace"

task :update do |t|
	system "echo '*************'"
	puts Time.now
	system "ruby -r '#{SCRIPT_ROOT}/script.rb' -e 'update_html'"
end
